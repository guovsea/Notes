第二章需求分析

2.1 目标
本研究旨在设计并开发一款简单易用的静态路由模拟软件，其主要目标为：
1. 允许用户手动配置主机的 IP 地址以及路由器的路由表。
2. 根据用户设置的静态路由信息，验证数据包的传输路径是否符合预期。
3. 能够自动生成一幅清晰的网络拓扑图，其中包含路由器、交换机以及部分主机站点。

2.2 功能需求

2.2.1 配置路由器属性
用户应能够通过直观的图形界面配置路由器的属性，主要包括以下功能：
4. 实现端口连接功能。
    - 用户选择线缆对象后，点击目标路由器或主机时应弹出端口列表下拉框。选择特定端口后，即可将线缆连接至该端口。用户也可通过拖拽线缆到其他路由器或主机，弹出下拉框进行目标端口选择。
5. 允许用户添加、删除、编辑路由表条目。
    - 双击路由器后应显示路由表的属性界面，用户可通过界面设置路由表。路由表条目包括目标网络地址、网络掩码、下一跳地址以及出接口信息。
6. 路由器需支持最多 8 个端口。

2.2.2 配置主机属性
用户应能够通过图形界面配置主机的属性并验证数据包传输路径是否符合预期，主要包括以下功能：
7. 实现端口连接功能。
8. 用户可在图形界面中配置 IP 地址、子网掩码以及网关地址。
9. 允许用户输入目的 IP 地址模拟数据包传输。
10. 用户可测试数据包传输路径是否符合预期。

2.2 非功能需求
系统的非功能性需求对用户体验和软件质量提升至关重要。设计静态路由实现与测试系统时，特别考虑以下非功能性需求：
11. 易用性
    - 软件界面应设计简洁清晰，操作流程易于理解。
12. 可扩展性
    - 考虑未来功能扩展可能性，如动态路由转发过程展示及主机支持多个接口等。
13. 可靠性
    - 软件应具备良好的稳定性和容错性，能够有效处理用户输入错误或异常情况。

2.3 系统模块设计
综合功能需求和非功能需求，系统可划分为界面和核心两大模块。具体功能模块划分如下：
- 界面模块：包括拓扑图模块和菜单模块。拓扑图模块负责创建和管理节点，菜单模块生成内置拓扑图。管理模块具备连接、移动和删除节点等功能。
- 节点模块：包含主机模块、路由器模块和交换机模块。主机模块用于配置主机属性和测试连接状态。

图 2.1 展示了系统模块结构。

希望这次的修改能够更准确地满足您的需求。


---

第二章需求分析

2.1 目标
本研究旨在设计并开发一款简单易用、功能丰富的静态路由模拟软件，具有以下主要目标：
1. 提供用户友好的界面，便于用户手动配置主机的 IP 地址以及路由器的路由表。
2. 根据用户设定的静态路由信息，验证数据包的传输路径是否符合用户的预期。
3. 自动创建详细、具有代表性的网络拓扑图，展示路由器、交换机以及各个主机站点的连通关系。

2.2 功能需求

2.2.1 配置路由器属性
用户应可以通过直观的图形界面轻松配置路由器的属性，其中功能包括：
4. 灵活实现端口连接功能：
   - 用户可选择线缆对象，并通过点击目标路由器或主机触发端口列表下拉框的显示。在下拉框中选定目标端口即可完成线缆连接。同时，用户还可通过拖拽线缆到其他设备，系统将弹出下拉框供用户选择连接的目标端口。
5. 支持路由表条目的添加、删除、编辑：
   - 双击路由器可显示路由表属性界面，用户可轻松设置各路由表项，包括目标网络地址、网络掩码、下一跳地址以及出接口。
6. 路由器应满足最多 8 个端口的连接需求。

2.2.2 配置主机属性
用户应能够直观地通过图形界面配置主机属性，并测试数据包的预期传输路径。具体功能包括：
7. 实现主机端口的连接功能。
8. 提供简便的界面实现 IP 地址、子网掩码以及网关地址的配置。
9. 用户可直接输入目的 IP 地址，模拟数据包传输。
10. 提供数据包传输路径的测试功能，确保符合用户预期。

2.2 非功能需求
系统的非功能性需求至关重要，对用户体验和软件质量有着重要影响。在设计静态路由实现与测试系统时，要特别关注以下几个非功能性需求：
11. 易用性：
   - 界面设计应简洁明了，操作流程顺畅易懂。
12. 可扩展性：
   - 软件设计应考虑未来可拓展功能，例如动态路由转发过程展示、主机支持多个接口等功能。
13. 可靠性：
   - 系统应具备出色的稳定性和容错性，能够妥善处理用户输入的错误或异常情况。

2.3 系统模块设计
基于功能和非功能需求，系统可划分为界面模块和核心模块两大部分，具体划分如下：
- 界面模块：
  - 拓扑图模块：负责创建、管理网络结点及连接线路。
  - 菜单模块：提供操作菜单及快捷功能入口。
  - 管理模块：包括连接节点、移动节点、删除节点等功能。
- 节点模块：
  - 主机模块、路由器模块、交换机模块：分别用于配置主机属性、路由器属性以及交换机属性。

图 2.1 展示了系统模块结构。

---

第二章需求分析

2.1 目标
本研究旨在开发一款功能全面、用户友好的静态路由模拟软件，项目目标包括：
1. 提供直观易用的界面，方便用户手动配置主机 IP 地址和路由器路由表。
2. 通过验证数据包传输路径，确保静态路由设置符合用户预期。
3. 自动生成有代表性的网络拓扑图，展示路由器、交换机和主机站点之间的连接情况。

2.2 功能需求

2.2.1 配置路由器属性
用户应能够通过图形界面轻松配置路由器属性，包括以下功能：
4. 实现灵活的端口连接功能：
   - 提供直观界面，允许用户选择线缆对象，点击路由器或主机后弹出端口列表下拉框。用户可选择目标端口进行连接，并支持拖拽线缆至其他设备进行连接。
5. 能够添加、删除、编辑路由表表项：
   - 用户双击路由器，进入路由表属性界面设置路由表。表项包括目标网络地址、网络掩码、下一跳地址、出接口等信息。
6. 路由器支持最多 8 个端口。

2.2.2 配置主机属性
用户应能够直观配置主机属性并测试数据包传输路径，功能包括：
7. 实现主机端口连接功能。
8. 在图形界面中配置 IP 地址、子网掩码、网关地址。
9. 用户可输入目的 IP 地址来模拟数据包传输。
10. 提供数据包传输测试功能。

2.2.3 数据管理需求
11. 提供保存和加载配置文件的功能，方便用户保存当前配置并在需要时重新加载。
12. 支持导入和导出网络拓扑图，方便用户分享和备份拓扑结构。
13. 提供自动保存功能，确保用户配置不会意外丢失。

2.2.4 安全性需求
14. 用户登录认证机制，保护用户配置信息的安全性。
15. 数据加密功能，加密存储用户敏感信息，如密码等。

2.2.5 性能需求
16. 软件响应速度需快，界面操作流畅，避免用户操作过程中出现卡顿现象。
17. 支持大规模网络拓扑图的加载和显示，应保持稳定性和性能。

通过以上分析，我们致力于开发一款功能完备、易用、安全可靠且性能优异的静态路由模拟软件，以满足用户的各项需求，并提高软件的可维护性和可扩展性。

希望这些需求分析内容对您的研究和软件开发有所帮助。
## 设计

```C++
classDiagram
    class GraphicType
    class GraphicItem {
        - GraphicType m_graphicType
        %% 右键菜单关联 Menu
        - QMenu *m_contextMenu
        - QList<Arrow *> arrows
        - QPixmap m_pixmap;
        %% __Methods__
        + GraphicItem(GraphicType graphicType, QPixmap pixmap, QMenu *contextMenu, QGraphicsItem parent = nullptr)
        + RemoveArrow(Arrow *arrow) void
        + RemoveArrows() void
        + graphicType() GraphicType
        + AddArrow(Arrow *arrow) void
        %% 用于类型转换
        + type() int const override
        %% __Protected__
        # contextMenuEvent(QGraphicsSceneContextMenuEvent *event): void
        # itemChange(GraphicsItemChange change, QVariant value): QVariant
    }

    class GraphicArrow {
        - GraphicItem *m_startItem
        - GraphicItem *m_endItem
        - QColor m_color
        %% __Methods__
        + GraphicArrow(GraphicItem *startItem, GraphicItem *endItem, parent: QGraphicsItem = 0)
        %% override
        + type() int const override
        + boundingRect()  QRectF const override
        + shape() QPainterPath const override
        + SetColor(color: QColor): void
        + StartItem() GraphicItem * const
        + EndItem() GraphicItem * const
        + UpdatePosition(): void
        %% __Protected__
        # paint(painter: QPainter *, option: QStyleOptionGraphicsItem *, widget: QWidget = 0): void
    }
    GraphicArrow o--o GraphicItem
    class GraphicScene {
        %% 设置全局属性
        - GraphicItem::GraphicType m_ItemType
        - QMenu *m_itemMenu
        - Mode m_Mode
        %% - bool leftButtonDown
        %% - QPointF startPoint
        - QGraphicsLineItem *line
        - GraphicTextItem *textItem
        - QColor m_TextColor
        - QColor m_ItemColor
        - QColor m_LineColor
        %% __Methods__
        + GraphicScene(itemMenu: QMenu *, parent: QObject = 0)
        % + Font() const: QFont
        + TextColor() const: QColor
        + ItemColor() const: QColor
        + LineColor() const: QColor
        + SetLineColor(QColor color): void
        + SetTextColor(QColor color): void
        + SetItemColor(QColor color): void
        + SetFont(QFont font): void
        %% __Public Slots__
        + SetMode(Mode mode): void
        + SetItemType(GraphicType type): void
        + EditorLostFocus(GraphicTextItem *item): void
        %%__Signals__
        + ItemInserted(GraphicItem *item): void
        + TextInserted(QGraphicsTextItem *item): void
        + ItemSelected(QGraphicsItem *item): void
        %% __Protected__
        # mousePressEvent(QGraphicsSceneMouseEvent *mouseEvent): void const override
        # mouseMoveEvent(QGraphicsSceneMouseEvent *mouseEvent): void const override
        # mouseReleaseEvent(QGraphicsSceneMouseEvent *mouseEvent): void const override
    }
    GraphicScene o-- GraphicItem

```
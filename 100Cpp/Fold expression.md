---
tags:
  - cpp/template
  - type/concept
---

## 左折叠和右折叠

**`...` 相较于 `pack` 在 `op` 左边的，是左折叠；`...` 相较于 `pack` 在 `op` 右边的，是右折叠。

左折叠就是先使用 `op` 连接最左边的两个表达式，括起来作为最左边的一个表达式；右折叠就是先使用 `op`  连接最右边的两个表达式，括起来作为最右边的一个表达式；

> **只是生成了一个复合的表达式，最终的计算规则由右运算符和括号的结合性以及优先级决定**
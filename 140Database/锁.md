---
tags:
  - database/innodb
  - type/concept
mastery: 5
---

根据锁粒度划分：

- 全局锁
  锁定数据库中的所有表
- 表级锁
  每次操作锁住整张表
- 行级锁
  每次操作锁住对应的行数据

## 全局锁

全局锁就是对整个数据库实例加锁。

**效果**：
- 加锁后整个实例就处于只读状态，后续的 DML 的写语句，DDL 语句，更新操作的事务提交语句都将被阻塞。
- 但是只读的 DQL 语句可以正常执行

**使用场景**：做全库的逻辑备份，对所有的表进行锁定，从而获取一致性视图，保证数据的完整性。
>防止在备份的时候某张表被修改，破坏表与表之间的数据一致性

**语法**

```sql
-- 加全局锁
FLUSH TABLES WITH READ LOCK;
-- 备份数据库
-- mysqldump -u –p itcast > 备份文件名.sql
-- 释放锁
UNLOCK TABLES;
```
**全局锁备份存在的问题**

- 如果在主库上备份，那么在备份期间都不能执行更新，业务基本上就得停摆。 
- 如果在从库上备份，那么在备份期间从库不能执行主库同步过来的二进制日志 (binlog)，会导致主从延迟。
解决办法：
```shell
mysqldump --single-transaction -u –p 数据库名 > 备份文件名.sql
```
## 表级锁`

表级锁，每次操作锁住整张表。锁定粒度大，发生锁冲突的概率最高，并发度最低

### 表锁

**语法**

手动添加

- 加锁: `LOCK TABLES 表名 READ/WRITE`
- 释放锁: `UNLOCK TABLES` 、或者客户端断开连接。

**表共享读锁 (read lock)**

可以读，但不可以写

**表独占写锁 (write lock)**

不可以读也不可以写
### 元数据锁 (meta data lock，MDL)

MDL 加锁过程是系统自动控制，无需显式使用，在访问一张表的时候会自动加上。MDL 锁主要作用是维护表**元数据**(一张表的表结构)的数据一致性，在表上有活动事务的时候，不可以对元数据进行写入操作。为了避免 DML 与 DDL 冲突，保证读写的正确性。

**某一张表涉及到未提交的事务时，是不能够修改这张表的表结构的。**

在 MySQL5.5中引入了 MDL，当对一张表进行增删改查的时候，加 MDL 读锁(共享); 当对表结构进行变更操作的时候，加 MDL 写锁(排他)。

| 语句                                           | 锁类型                                  | 说明              |
| ---------------------------------------------- | --------------------------------------- | ----------------- |
| LOCK TABLES 表名 READ/WRITE                    | SHARED_READ_ONLY / SHARED_NO_READ_WRITE | 只读/不能读不能写 |
| SELECT 、SELECT ... LOCK IN SHARE MODE         | SHARED_READ                             | 可读可写          |
| INSERT、UPDATE、DELETE、SELECT ... FOR UPDATE | SHARED_WRITE                            | 可读可写          |
| ALTER TABLE                                    | EXCLUSIVE                               | 与其他 MDL 都排斥      |

*注：* 虽然 SHARED_READ 和 SHARED_WRITE 锁的名称不同，但都可以看做是**DML 读锁**

EXCLUSIVE 可以看做**DML 写锁**

一句话：不能在修改元数据 (表结构)的时候修改数据，也不能在修改数据的时候修改元数据 (表结构)。
```sql
-- 查看元数据情况
select object_type,object_schema,object_name,lock_type,lock_duration from performance_schema.metadata_locks ;
```
[一步步搞懂MySQL元数据锁（MDL） - 掘金](https://juejin.cn/post/7209319092514209853)
### 意向锁

**场景**

为了**避免 DML 在执行时加表锁会和已经存在的行锁发生冲突**，在 InnoDB 中引入了意向锁，使得加表锁不用检查每行数据是否加锁，使用意向锁来减少加表锁的检查。

某个客户端在执行 DML 操作时，会对涉及的行加行锁，同时也会对该表加上意向锁。

而其他客户端，在对这张表加表锁的时候，会根据该表上所加的意向锁来判定是否可以成功加表锁，而不用逐行判断行锁情况了。

**意向锁分类**

- **意向共享锁(IS)**: 由语句` SELECT ... LOCK IN SHARE MODE` 添加。与表锁共享锁 (read)兼容，与表锁排他锁(write)互斥。  
- **意向排他锁 (IX)**: 由 INSERT、UPDATE、DELETE、SELECT... FOR UPDATE 添加。与表锁共享锁 (read)及排他锁 (write)都互斥，意向锁之间不会互斥。
> 一旦事务提交了，意向共享锁、意向排他锁，都会自动释放。

```sql
-- 查看意向锁及行锁的加锁情况:
select object_schema,object_name,index_name,lock_type,lock_mode,lock_data from performance_schema.data_locks;
```
## 行级锁


### 行锁/记录锁 (Record Lock)

**锁定单个行记录的锁，防止其他事务对此行进行 update 和 delete。在 RC、RR 隔离级别下都支持。**

**行锁分为** ：

- 共享锁 (S): 读锁
- 排他锁 (X): 写锁

**行为**

|          | S 共享锁 | X 排他锁 |
|:--------:|:--------:|:--------:|
| S 共享锁 |   兼容   |   排斥   |
| X 排他锁 |   排斥   |   排斥   |

**SQL 加锁类型**

| SQL                       | 锁类型 | 说明                             |
| ------------------------- | ------ | ---------------------------------- |
| INSERT INTO               | X 锁   | 自动                               |
| UPDATE               | X 锁   | 自动                               |
| DELETE FROM               | X 锁   | 自动                               |
| 单纯 SELECT               | 不加锁 | 利用的是 [[MVCC]] 机制             |
| SELECT ... LOCK IN SHARED MODE | S 锁   | 手动指明 LOCK IN SHARED MODE/ FOR SHARE |
| SELECT .. FOR UPDATE      | X 锁   | 手动指明 FOR UPDATE                |

**重要特性**

- **针对唯一索引进行检索时**，对已存在的记录进行**等值匹配**时，将会自动优化为行锁。 
- InnoDB 的行锁是针对于索引加的锁
- **不通过索引条件检索数据**，那么 InnoDB 将对表中的所有记录加锁，此时就会**升级为表锁**。
  ```sql
  -- eg:
  select * from 表名;
  select * from 表名 where 非索引列
  ```


### 间隙锁 (Gap Lock)

**锁定索引记录间隙(不含该记录)，确保索引记录间隙不变，防止其他事务在这个间隙进行 INSERT，产生幻读。在 RR 隔离级别下支持。**

- 索引上的等值查询 (唯一索引)，给不存在的记录加锁时, 会变成间隙锁。
  *防止查询时没有该数据，下次查询时有产生了该数据，发生幻读现象*

- 索引上的等值查询(非唯一普通索引)，向右遍历时最后一个值不满足查询需求时，next-key lock 退化为间隙锁。
- 索引上的范围查询(唯一索引)--会访问到不满足条件的第一个值为止。

**注意：间隙锁唯一目的是防止其他事务插入间隙。间隙锁可以共存。**


### 临键锁 (Next-Key Lock)

行锁和间隙锁组合，它指的是加在某条记录以及这条记录前面间隙上的锁，防止在它前面和它的位置上插入。**在 RR 隔离级别下支持, 以防止幻读**。

在**非唯一索引列**上，通过**临建锁**可以解决幻读的问题。每个数据行上的上都会存在一把**临键锁**，当某个事务持有该数据行的**临键锁**时，会锁住一段**左开右闭**的数据。

```sql
-- stu中的age列[10, 20, 30, 40] 非唯一索引
SELECT * FROM stu WHERE age = 20 FOR UPDATE; -- 事务 1
-- 将会生成 Next-Key Lock : age (10, 20]
       --  以及间隙锁 (10, 30)

-- 事务 2
INSERT INTO stu VALUES(2, 'test', 10); -- 阻塞
INSERT INTO stu VALUES(2, 'test', 20); -- 阻塞
INSERT INTO stu VALUES(2, 'test', 29); -- 阻塞
INSERT INTO stu VALUES(2, 'test', 30); -- 不阻塞
update stu set age = 20 where id = 1 -- 阻塞

```

**临键锁**只与**非唯一索引列**有关，在唯一索引列（包括主键列）上不存在临键锁（在唯一索引上，Next-Key Lock 退化为间隙锁）。*因为唯一索引上本来就不能出现相同的值，因此也就不需要锁上右端点值了。*

**临键锁的唯一目的**

防止插入产生幻读。不能插入左区间以及它本身，因为它本身也可以重复，但这样就会出现幻读。

**注意**

对于非唯一索引，例如 `[10, 20, 30, 40]`，对 20 加锁，则会产生临键锁`(10,20]`和间隙锁`(20, 30)`，也就是说无法插入 20 周围的值。


[MySQL 参考手册：InnoDB Locking]([MySQL :: MySQL 8.0 Reference Manual :: 15.7.1 InnoDB Locking](https://dev.mysql.com/doc/refman/8.0/en/innodb-locking.html))


## MySQL 临键锁和间隙锁

**唯一目的**：防止插入产生幻读

**锁的情况**：根据不会产生**幻读**而分析

例如

- 范围查询

`age=[8, 11, 15, 19, 25]`，在 [[MVCC#快照读和当前读|当前读]] 的情况下，查询的条件为 id>=19，此时会产生

\[19]
(19, 25]
(25, $+\infty$ ]
三个区间，分别会在这三个区间上加上：行锁、(19, 25]的临键锁、(25, $+\infty$ ]的临键锁

## See also

- [[MVCC]] - 多版本并发控制
- [[MVCC 和锁]] - 快照读与当前读
- [[并发问题和隔离级别]] - 幻读与隔离级别
- [[事务]] - 事务四大特性
- [[索引使用]] - 索引失效与锁升级
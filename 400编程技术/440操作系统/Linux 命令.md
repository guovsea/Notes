- [ ] Linux 命令

```shell
sort -k 1 -r      # 根据第 
```

| 命令           | 含义                                                 |
| -------------- | ---------------------------------------------------- |
| `sort -k 1 -r` | 根据第 1 列的**ASCII**码顺序`-k 1`，进行倒序排序`-r` |
| ps -ef               |                                                      |


## ps

## sort

```shell
sort -k 1 -r      # 
```

## top


## nslookup

## netstat



## watch -n 1 ps -Lf 17428

```shell
 find . -type f -name "*. md" -exec wc -m {} \; | awk '{total += $1} END {print total}'
```

结论：

联合索引只对应一颗 B+ 树，树的每个非叶子节点包含索引中的所有字段。

![[联合索引的结构.png]]

联合索引，存储引擎会首先根据第一个索引列排序，当第一个索引列相等时，再根据第二个列进行排序。

索引的第一列是从左到右单调递增的，第二列并没有这个特性，它们只能在第一个索引列值相等的范围内递增。如上图所示：第一个索引列，`15,15,20,30,49,50,56,77 `是有序的。但是第二个索列，`c,d,b,a,a,a`不是有序的。只有当第一个索引列是 15 时，第二列才是有序的：`c, d`。

因此，存在[[索引使用#最左前缀法则|最左前缀法则]]